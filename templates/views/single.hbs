{{>header}}
    <div class="container text-center">
        <h1 class="h1 my-4">{{Mydata.title}}</h1>
        <img src="{{Mydata.urlToImage}}" class="img-fluid d-block mx-auto mb-5">
        <p>{{Mydata.description}}</p>
    </div>
{{>footer}}